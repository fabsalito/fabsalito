<div class="sidebar">
    <div class="row">
        <div class="span3 section-title">
            <span class="pull-right">
                <h4>Tags</h4>
            </span>
        </div>
    </div>
    <div class="row">
        <div class="span3">
            <p class="text-center">
                {% for tag, weight in tags %}
                    <span class="cloud-tag cloud-weight-{{ weight }}">{{ tag }}</span>
                {% else %}
                    <p class="text-info">There are no tags</p>
                {% endfor %}
            </p>
        </div>
    </div>

    <div class="row">
        <div class="span3 section-title">
            <span class="pull-right">
                <h4>Comments</h4>
            </span>
        </div>
    </div>

    <div class="row">
        <div class="span3">
            {% for comment in latestComments %}
                <article>
                    <p>
                        <small>
                            <span class="muted"><strong>{{ comment.user }}</strong> commented on</span>
                            
                            <a href="{{ path('fabsalito_blog_show', { 'id': comment.blog.id, 'slug': comment.blog.slug }) }}#comment-{{ comment.id }}">
                                {{ comment.blog.title }}
                            </a>
                            
                            [<em><span class="muted">
                                <time datetime="{{ comment.created|date('c') }}">{{ comment.created|created_ago }}</time>
                            </span></em>]
                        </small>
                    </p>
                    <p>
                        <small>
                            {{ comment.comment }}
                        </small>
                    </p>
                </article>
            {% else %}
                <p class="text-info">There are no recent comments</p>
            {% endfor %}
        </div>
    </div>
</div>